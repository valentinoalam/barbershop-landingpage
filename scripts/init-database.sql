-- Initialize database with sample data
-- This script will be run to set up the initial database structure and seed data

-- Note: Prisma will handle the table creation, this is just for reference
-- The actual schema is defined in prisma/schema.prisma

-- Sample data will be inserted via the seed script (prisma/seed.ts)
-- Run: npm run db:seed

-- To reset and reseed the database:
-- 1. npm run db:push (to sync schema)
-- 2. npm run db:seed (to populate with sample data)

SELECT 'Database initialization script - run prisma commands to set up the database' as message;
